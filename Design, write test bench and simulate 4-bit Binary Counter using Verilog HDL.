module counter4(clk, reset, q);
input clk, reset;
output reg [3:0] q;
always @(posedge clk or posedge reset)
begin
if(reset)
q <= 4'b0000;
else
q <= q + 1;
end
endmodule



Test bench
module tb_counter;
reg clk, reset;
wire [3:0] q;
counter4 uut(clk,reset,q);
always #5 clk = ~clk;
initialbegin
clk=0;
reset=1;
#10 reset=0;
#100 $finish;
end
endmodule